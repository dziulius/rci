<%= render :partial => "common/date_range_select", :locals => {:object => @department, :url => :department_users_path} %>

<% table_for User, @department.users_with_work_hours, :name, :work_hours, :own_work_hours, :foreign_work_hours, :lazy_hours %>
