<% table_for Project, @department.projects(params[:date_from], params[:date_to]), linked(:name), linked_to(:leader), linked_to(:department), :start_at, :end_at,
  :work_hours, :own_work_hours, :foreign_work_hours %>