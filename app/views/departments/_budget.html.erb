<%= label_tag :project_id %>
<%= select_tag :project_id, options_for_select([[]] + @department.projects.collect {|d| [d.name, d.id] }) %>

<% table_with_totals_for Budget, @department.budgets.all, :at_string, :hours, :used, :remaining %>
